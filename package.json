{
  "name": "fbo-airport",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "prebuild": "node scripts/build-price-history.mjs",
    "build": "next build",
    "build:cloudflare": "node scripts/build-price-history.mjs && next build && find out -name '*.txt' -delete && rm -rf .next node_modules",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "next": "16.1.6",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "babel-plugin-react-compiler": "1.0.0",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "puppeteer": "^24.37.5"
  }
}
